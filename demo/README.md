# Demo & Mock Data

Reliability is key. This folder contains fallback data to ensure the demo works even if the live API or AI fails during the presentation.

## Usage
Import these JSON files in your frontend/backend to serve static data when `DEMO_MODE=true`.
