
f, x, y, z, wall, slot, path, pillar, occupied, elevator, product_id, quantity



as an admin i can:
- log into the administrative dashboard
- Manages user accounts (create, assigne roles "admin, employee, supervisor, disable)
- receive ai suggestions and validate or override them by assigning alternative locations or routes and a justification
- monitor ai performance (forecast accuracy, optimization efficiency)
- review all audit logs (stock movements, overrides, validations)
- modify the warehouse structure and add new warehouses

as a supervisor i can:
- log into the system dashboard
- receive ai suggestions and validate or override them by assigning alternative locations or routes and a justification
- monitors zone occupancy
- create receipt operations specifying: the product, quantity, and employee who does the task
- create delivery operations specifying: the product, quantity, and employee who delivers
- view ai generated chariot path (visualized in each suggestion that suggests the path)
- receives reports about missing or damages products, location conflicts


as an employee i can:
- log into the employee dashboard (assigned tasks validated by the supervisor or the admin)
the tasks are:
- receipt operation:
	- checks if the product stated arrived, enter the quantity (the expected one by default)
	- Flags discrepancies if quantities differ.
	- validate the receipt (new info stored in db)
- storage operation:
	- view the instructions generated by the ai then validated by the supervisor or admin (exact floor and slot location and path)
	- validates the storage completion after transporting products to the assigned area 
- picking operation:
	- view the instructions generated by the ai then validated by the supervisor or admin (product, quantity, exact floor and slot location and) and also the exact rack location in the ground floor where to store it and the path (from the storage slot to the elevator, then from the elevator in the ground floor to the rack)
	- the employee validates when done and info updated in the system
- delivery operation:
	- view instructions sent by the supervisor (product and quantity)
	- the employee validates and info updated in the system


as an ai i can:
- predict required products and quantities a day before, then trigger picking operation specifying the product, quantity
- the second ai (or algorithm) then generates the path from the current location to the elevator, then from the elevator to the rack in the ground floor.
- the second ai generates the path after receipt to the exact floor and slot based on weight, demand frequency, slot
