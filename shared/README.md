# Shared Resources

Shared code between Frontend (Mobile) and Backend to prevent duplication.
Ideally, valid for TypeScript projects (using workspaces).

## Contents
- `/types`: TS Interfaces shared across the stack.
- `/constants`: Global constants (Role enums, Config keys).
- `/schemas`: Zod/Validation schemas.
